import { initialize } from '../../../../libs/utils/constructor-initializer'

export class UpgradeGoogleSharedDriveVersionCommand {
  constructor(props: UpgradeGoogleSharedDriveVersionCommand) {
    initialize(this, props)
  }
}
